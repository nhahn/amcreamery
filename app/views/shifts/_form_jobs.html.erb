<% if edit %>
  <ul class="unstyled">
    <% Job.active.alphabetical.all.each do |job| %>
      <li><%= check_box_tag "shift[job_ids][]",job.id,@shift.jobs.include?(job) %>
      <a class="job-popover" data-content="<%=job.description%>"><%= job.name %></a></li>
    <% end %>
  </ul>
<% end %>
